; Submission listens at :465 for TLS connections, and forwards these unencrypted
; to :25. Before forwarding to :25, a delay is applied, to allow health checks
; for this port without excessive mailserver logs. It is recommended to set the
; delay as close as possible to zero, without receiving ECONNRESET logs.
;delay_ms = 200
